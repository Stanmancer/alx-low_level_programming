#include <stdio.h>

/**
 * main - entry point
 * @argc: argument counter
 * @argv: array of pointers to char
 *
 * Return: 0
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}
